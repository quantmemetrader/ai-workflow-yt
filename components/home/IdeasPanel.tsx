"use client";

import type { Idea } from "@/lib/ideas/types";

/**
 * Home's ideas module: researched video ideas, each one a project away.
 *
 * Placeholder until the ideas panel lands; the props are the contract.
 */
export function IdeasPanel(props: { zh: boolean; initial: Idea[]; canStart: boolean }) {
  void props;
  return null;
}
