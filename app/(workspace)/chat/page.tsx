import { ModuleStub } from "@/components/ModuleStub";

export default function ChatPage() {
  return (
    <ModuleStub
      title="Chat"
      spec="Build-Spec Section 4.2"
      summary="One agent per employee. Streaming responses, tool-call transparency, inline file previews, and citations that link to the file. Can trigger any module the employee is entitled to."
    />
  );
}
